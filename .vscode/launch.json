{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug exe",
      "preLaunchTask": "build",
      "program": "${workspaceFolder}/core/zig-out/bin/tablemd",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
